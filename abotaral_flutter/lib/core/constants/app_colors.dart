import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary colors
  static const Color primary = Color(0xFF007644);
  static const Color primaryDark = Color(0xFF2D6A4F);

  // Accent / Secondary colors
  static const Color accent1 = Color(0xFFD8F3DC);
  static const Color accent2 = Color(0xFF8CC1B0);
  static const Color accent3 = Color(0xFF52B788);
  static const Color accent4 = Color(0xFF2D6A4F);

  // Background colors
  static const Color background = Colors.white;
  static const Color scaffoldBackground = Colors.white;

  // Text colors (Neutral)
  static const Color textPrimary = Color(0xFF181D27);
  static const Color textSecondary = Color(0xFF717680);
  static const Color textHint = Color(0xFF717680);

  // Border colors (Neutral)
  static const Color border = Color(0xFFD5D7DA);
  static const Color inputBorder = Color(0xFFD5D7DA);
  static const Color inputFill = Colors.white;

  // Feedback colors
  static const Color error = Color(0xFFF04438);
  static const Color warning = Color(0xFFF79009);
  static const Color success = Color(0xFF12B76A);

  // Link color
  static const Color link = Color(0xFF007644);
}
